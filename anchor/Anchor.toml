[features]
resolution = true
skip-lint = false

[programs.devnet]
coin_flip = "ReplaceWithDeployedProgramId"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "ts-mocha -p ./tsconfig.json -t 1000000 anchor/tests/**/*.ts"
